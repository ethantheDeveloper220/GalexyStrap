<ui:UiPage
    x:Class="Voidstrap.UI.Elements.Settings.Pages.ExternalToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="External Tools"
    Scrollable="True"
    d:DesignHeight="800"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <StackPanel Margin="0,0,0,16">
        <!-- Page Header -->
        <TextBlock FontSize="28" FontWeight="Bold" Text="External Tools" Margin="0,0,0,8"/>
        <TextBlock FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,24">
            Launch external programs automatically when Roblox starts. Add as many tools as you want.
        </TextBlock>

        <!-- Tools List -->
        <ui:CardControl Icon="Apps24" Header="Configured Tools" Margin="0,0,0,16">
            <StackPanel>
                <ListBox x:Name="ToolsListBox" 
                         MinHeight="200" 
                         MaxHeight="400"
                         Background="Transparent"
                         BorderThickness="0"
                         SelectionChanged="ToolsListBox_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <ui:SymbolIcon Grid.Column="0" Symbol="Apps24" FontSize="20" Margin="0,0,12,0" Foreground="#FF6666"/>
                                
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="{Binding Path}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Arguments}" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                </StackPanel>
                                
                                <ui:Button Grid.Column="2" 
                                          Content="Remove" 
                                          Appearance="Danger"
                                          Icon="Delete24"
                                          Click="RemoveTool_Click"
                                          Tag="{Binding}"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
                <TextBlock x:Name="NoToolsText" 
                          Text="No external tools configured. Click 'Add Tool' to get started." 
                          FontSize="13" 
                          Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                          HorizontalAlignment="Center"
                          Margin="0,20"
                          Visibility="Collapsed"/>
            </StackPanel>
        </ui:CardControl>

        <!-- Add New Tool -->
        <ui:CardExpander Margin="0,0,0,16" IsExpanded="False">
            <ui:CardExpander.Header>
                <StackPanel>
                    <TextBlock FontSize="14" Text="Add New Tool" />
                    <TextBlock Margin="0,2,0,0" FontSize="12" Text="Configure a new external tool to launch with Roblox" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </StackPanel>
            </ui:CardExpander.Header>
            
            <StackPanel>
                <TextBlock Text="Tool Name" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <ui:TextBox x:Name="ToolNameTextBox" 
                           Icon="Apps24"
                           PlaceholderText="e.g., Discord, OBS Studio, etc."
                           Margin="0,0,0,12"/>
                
                <TextBlock Text="Executable Path" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ui:TextBox x:Name="ToolPathTextBox" 
                               Grid.Column="0"
                               Icon="Folder24"
                               PlaceholderText="C:\Path\To\Program.exe"
                               Margin="0,0,8,0"/>
                    <ui:Button Grid.Column="1" 
                              Content="Browse" 
                              Icon="FolderOpen24"
                              Click="BrowseTool_Click"/>
                </Grid>
                
                <TextBlock Text="Arguments (Optional)" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <ui:TextBox x:Name="ToolArgumentsTextBox" 
                           Icon="Code24"
                           PlaceholderText="Command line arguments"
                           Margin="0,0,0,16"/>
                
                <ui:Button Content="Add Tool" 
                          Icon="Add24" 
                          Click="AddTool_Click"
                          Appearance="Primary"/>
            </StackPanel>
        </ui:CardExpander>

        <!-- Status -->
        <ui:CardControl Icon="Info24" Header="Status">
            <TextBlock x:Name="StatusText" 
                      Text="External tools will launch automatically when Roblox starts." 
                      FontSize="13" 
                      TextWrapping="Wrap"/>
        </ui:CardControl>
    </StackPanel>
</ui:UiPage>
