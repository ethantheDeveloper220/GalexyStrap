<base:WpfUiWindow
    x:Class="Voidstrap.UI.Elements.PerformanceMonitor.PerformanceMonitorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:Voidstrap.UI.Elements.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Performance Monitor - GalaxyStrap"
    Width="900"
    Height="650"
    MinWidth="700"
    MinHeight="500"
    Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen"
    Topmost="{Binding AlwaysOnTop}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="Performance Monitor"
                     Icon="pack://application:,,,/GalaxyStrap.ico"
                     ShowMaximize="True"
                     ShowMinimize="True" />

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- Header -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <ui:SymbolIcon Symbol="ChartMultiple24" FontSize="32" Margin="0,0,12,0" Foreground="{DynamicResource SystemAccentBrush}"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Real-Time Performance Monitor" FontSize="24" FontWeight="Bold"/>
                        <TextBlock Text="Monitor Roblox and system performance metrics" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Control Buttons -->
                <WrapPanel Margin="0,0,0,16">
                    <ui:Button Content="Start Monitoring" 
                              Icon="Play24" 
                              Appearance="Primary"
                              Click="StartMonitoring_Click"
                              Margin="0,0,8,0"
                              x:Name="StartButton"/>
                    <ui:Button Content="Stop Monitoring" 
                              Icon="Stop24"
                              Click="StopMonitoring_Click"
                              Margin="0,0,8,0"
                              x:Name="StopButton"
                              IsEnabled="False"/>
                    <ui:Button Content="Reset Stats" 
                              Icon="ArrowReset24"
                              Click="ResetStats_Click"
                              Margin="0,0,8,0"/>
                    <ToggleButton Content="Always On Top"
                                 IsChecked="{Binding AlwaysOnTop, Mode=TwoWay}"
                                 Margin="0,0,8,0"
                                 Padding="10,5"/>
                </WrapPanel>

                <!-- Status Indicator -->
                <Border Background="{DynamicResource ControlFillColorDefaultBrush}" 
                       CornerRadius="8" 
                       Padding="12" 
                       Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse x:Name="StatusIndicator" Width="12" Height="12" Fill="Gray" Margin="0,0,8,0"/>
                        <TextBlock x:Name="StatusText" Text="Monitoring Stopped" FontWeight="SemiBold"/>
                        <TextBlock x:Name="RobloxStatusText" Text="" Margin="20,0,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </Border>

                <!-- Metrics Grid -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- CPU Usage -->
                    <ui:CardControl Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" Icon="Desktop24">
                        <StackPanel>
                            <TextBlock Text="CPU Usage" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBlock x:Name="CpuUsageText" Text="0%" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource SystemAccentBrush}"/>
                            <ProgressBar x:Name="CpuProgressBar" Height="8" Margin="0,8,0,4" Maximum="100"/>
                            <TextBlock x:Name="CpuDetailsText" Text="Idle" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                        </StackPanel>
                    </ui:CardControl>

                    <!-- Memory Usage -->
                    <ui:CardControl Grid.Row="0" Grid.Column="1" Margin="8,0,0,8" Icon="SaveMultiple24">
                        <StackPanel>
                            <TextBlock Text="Memory Usage" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBlock x:Name="MemoryUsageText" Text="0 MB" FontSize="32" FontWeight="Bold" Foreground="#10B981"/>
                            <ProgressBar x:Name="MemoryProgressBar" Height="8" Margin="0,8,0,4" Maximum="100"/>
                            <TextBlock x:Name="MemoryDetailsText" Text="0 MB / 0 MB" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                        </StackPanel>
                    </ui:CardControl>

                    <!-- FPS -->
                    <ui:CardControl Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" Icon="VideoClip24">
                        <StackPanel>
                            <TextBlock Text="FPS (Frames Per Second)" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBlock x:Name="FpsText" Text="0 FPS" FontSize="32" FontWeight="Bold" Foreground="#F59E0B"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Avg: " FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock x:Name="AvgFpsText" Text="0" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock Text=" | Min: " FontSize="11" Margin="8,0,0,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock x:Name="MinFpsText" Text="0" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock Text=" | Max: " FontSize="11" Margin="8,0,0,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock x:Name="MaxFpsText" Text="0" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </ui:CardControl>

                    <!-- Network/Ping -->
                    <ui:CardControl Grid.Row="1" Grid.Column="1" Margin="8,0,0,8" Icon="Wifi124">
                        <StackPanel>
                            <TextBlock Text="Network Ping" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBlock x:Name="PingText" Text="0 ms" FontSize="32" FontWeight="Bold" Foreground="#8B5CF6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Avg: " FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock x:Name="AvgPingText" Text="0 ms" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock Text=" | Packet Loss: " FontSize="11" Margin="8,0,0,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock x:Name="PacketLossText" Text="0%" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </ui:CardControl>

                    <!-- GPU Usage -->
                    <ui:CardControl Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" Icon="DeveloperBoard24">
                        <StackPanel>
                            <TextBlock Text="GPU Usage" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBlock x:Name="GpuUsageText" Text="0%" FontSize="32" FontWeight="Bold" Foreground="#EF4444"/>
                            <ProgressBar x:Name="GpuProgressBar" Height="8" Margin="0,8,0,4" Maximum="100"/>
                            <TextBlock x:Name="GpuDetailsText" Text="GPU Name" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                        </StackPanel>
                    </ui:CardControl>

                    <!-- Disk Usage -->
                    <ui:CardControl Grid.Row="2" Grid.Column="1" Margin="8,0,0,8" Icon="HardDrive24">
                        <StackPanel>
                            <TextBlock Text="Disk Activity" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBlock x:Name="DiskUsageText" Text="0 MB/s" FontSize="32" FontWeight="Bold" Foreground="#06B6D4"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Read: " FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock x:Name="DiskReadText" Text="0 MB/s" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock Text=" | Write: " FontSize="11" Margin="8,0,0,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock x:Name="DiskWriteText" Text="0 MB/s" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </ui:CardControl>
                </Grid>

                <!-- Session Stats -->
                <ui:CardExpander Margin="0,8,0,0" IsExpanded="True">
                    <ui:CardExpander.Header>
                        <StackPanel>
                            <TextBlock Text="Session Statistics" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="Overall performance metrics for this session" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </ui:CardExpander.Header>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Session Duration" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <TextBlock x:Name="SessionDurationText" Text="00:00:00" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="8,0,8,0">
                            <TextBlock Text="Average CPU" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <TextBlock x:Name="AvgCpuText" Text="0%" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="8,0,0,0">
                            <TextBlock Text="Peak Memory" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <TextBlock x:Name="PeakMemoryText" Text="0 MB" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </ui:CardExpander>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</base:WpfUiWindow>
